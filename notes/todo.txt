TODO:
- Hook up route params to components
- All params are required to be optional in the route as they are all using weakObj
  - Either set so it's always optional
  - Or fix so when calling getRoute or update, it uses the generic instead of WeakObj
- Keyboard shortcuts
- Update to newest CRA
- Add history
  - Either using to, or an event listener
- Replace string math with your own math function
- Fix input styling
- Fix createRoute using an increment id
- Automate component/index.tsx creation

